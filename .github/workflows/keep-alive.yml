name: Keep app awake

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Visit the app
        run: curl -sS --max-time 30 https://finance-ai-advisor-2iiincgxwruua8zfqmqf4t.streamlit.app/ || true
